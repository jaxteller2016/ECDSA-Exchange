import User from "./User";





users.forEach((user, index) => {
users[index] = new User();
users.push(user[index]);
console.log(users);
});


export default Users;
// 'Index: ' + index + ' Value: ' + users[index]

// 'Adress ' + this.address + ' is ' + this.address.length + ' code units long'